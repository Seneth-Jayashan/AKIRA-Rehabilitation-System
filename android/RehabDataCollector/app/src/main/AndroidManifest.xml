<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Legacy Bluetooth (API <= 30) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Android 12+ (API 31+) runtime Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- This app is local-only by design (research tool, no login, no cloud sync),
         so it intentionally does NOT request INTERNET. If you later add cloud sync,
         camera-based video capture, or a foreground recording notification, add
         INTERNET/ACCESS_NETWORK_STATE, CAMERA/RECORD_AUDIO, or POST_NOTIFICATIONS
         back here alongside the feature that needs them. -->

    <application
        android:name=".RehabApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Rehab Data Collector"
        android:supportsRtl="true"
        android:theme="@style/Theme.RehabDataCollector">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RehabDataCollector"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
