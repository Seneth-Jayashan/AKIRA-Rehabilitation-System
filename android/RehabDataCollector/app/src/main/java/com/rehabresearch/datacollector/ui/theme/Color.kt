package com.rehabresearch.datacollector.ui.theme

import androidx.compose.ui.graphics.Color

// Clinical, calm palette — this is a research tool, not a consumer app.
// Avoid anything flashy; therapists will stare at this during live sessions.
val ClinicalBlue = Color(0xFF1D6FA5)
val ClinicalTeal = Color(0xFF0F9B8E)
val BackgroundGray = Color(0xFFF5F7FA)
val SurfaceWhite = Color(0xFFFFFFFF)
val AlertRed = Color(0xFFD64550)
val WarningAmber = Color(0xFFE8A33D)
val SuccessGreen = Color(0xFF3FA34D)
val TextPrimary = Color(0xFF1B2430)
val TextSecondary = Color(0xFF5C6B7A)

// Axis colors for IMU charts — keep consistent across every chart in the app
val AxisXColor = Color(0xFFE4572E) // Ax / Gx
val AxisYColor = Color(0xFF17BEBB) // Ay / Gy
val AxisZColor = Color(0xFF2E4057) // Az / Gz
